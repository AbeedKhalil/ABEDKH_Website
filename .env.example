PORT=3000
JWT_SECRET=change-me-to-at-least-32-random-bytes-for-real

DB_PATH=Backend/data/portfolio.sqlite
UPLOADS_DIR=Backend/uploads

# Comma-separated allowlist of origins permitted to call the API cross-origin.
# Empty = allow all (local dev). Production value lives in the Render dashboard.
# Example: CORS_ORIGINS=https://abedkh.com,https://www.abedkh.com
CORS_ORIGINS=
